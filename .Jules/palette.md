## 2026-05-11 - Accessible CSS Bar Charts
**Learning:** Custom visual elements like CSS bar charts are opaque to screen readers by default.
**Action:** Add `role="progressbar"` and ARIA attributes (`aria-valuenow`, `aria-label`, `aria-valuemin`, `aria-valuemax`) to make custom visualizations accessible.
